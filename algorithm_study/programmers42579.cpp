#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;

struct music {
    string genres;
    int playCnt;
};

bool genresCompare(music music1, music music2) {

}

vector<int> solution(vector<string> genres, vector<int> plays) {
    vector<int> answer;


    return answer;
}