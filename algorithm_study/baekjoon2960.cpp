#include <iostream>
#include <vector>
using namespace std;
//yet
int main() {
	int n, k;
	cin >> n >> k;
	vector<int> v;
	for (int i = 2; i < n; i++) {
		v.push_back(i + 1);
	}
	for (auto iter1 = v.begin(); iter1 != v.end(); iter1++) {
		
	}
}